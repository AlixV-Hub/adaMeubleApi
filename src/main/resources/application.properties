spring.application.name=meubles-backend

server.port=${PORT:8080}

spring.datasource.url=jdbc:postgresql://${DB_HOST:db.tyfiqegwjdynefehgefr.supabase.co}:${DB_PORT:5432}/${DB_NAME:postgres}?sslmode=require
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:qCk91VFundh3uvpY}
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

jwt.secret=${JWT_SECRET:SuperSecretJWTKey123456789ABCDEFGHIJKLMNOPQRSTUVWXYZ}
jwt.expiration=86400000

cors.allowed.origins=${FRONTEND_URL:http://localhost:3000}

logging.level.org.hibernate.SQL=WARN
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=WARN
