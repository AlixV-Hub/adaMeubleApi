spring.application.name=meubles-backend
server.port=${PORT:8080}

# Base de données Supabase (connexion directe - SIMPLE)
spring.datasource.url=jdbc:postgresql://db.tyfiqegwjdynefehgefr.supabase.co:5432/postgres
spring.datasource.username=postgres
spring.datasource.password=1ejla05FxXhY9ume
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# SSL obligatoire
spring.datasource.hikari.data-source-properties.ssl=true
spring.datasource.hikari.data-source-properties.sslmode=require

# JWT
jwt.secret=SuperSecretJWTKey123456789ABCDEFGHIJKLMNOPQRSTUVWXYZ
jwt.expiration=86400000

# CORS
cors.allowed.origins=http://localhost:3000

# Logging
logging.level.org.hibernate.SQL=WARN